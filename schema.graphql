type playerData {
	type: String
	description: String
	author: String
	ethic: String
	moral: String
}

type Query {
	playerData(type: String, moral: String, ethic: String): [playerData!]
	allPlayerData: [playerData!]
}
